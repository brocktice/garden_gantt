---
phase: 02-data-layer-first-end-to-end
plan: 07
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - src/ui/cn.ts
  - src/ui/Button.tsx
  - src/ui/Input.tsx
  - src/ui/Label.tsx
  - src/ui/Select.tsx
  - src/ui/Dialog.tsx
  - src/ui/DropdownMenu.tsx
  - src/ui/Switch.tsx
  - src/ui/Toast.tsx
  - src/ui/Card.tsx
  - src/ui/Badge.tsx
  - src/index.css
  - package.json
autonomous: true
requirements: [CAT-03, CAT-04, CAT-05, LOC-05]
must_haves:
  truths:
    - "src/ui/cn.ts exports cn(...inputs: ClassValue[]): string using clsx + tailwind-merge"
    - "src/ui/{Button,Input,Label,Select,Dialog,DropdownMenu,Switch,Toast,Card,Badge}.tsx all exist with named exports"
    - "Radix primitives are used for Dialog/DropdownMenu/Select/Switch/Toast (no hand-rolled focus traps)"
    - "Tailwind v4 @theme tokens for Phase 2 spacing tokens (--spacing-card-min, --spacing-modal-max-w, --spacing-panel-w, --spacing-pill-h, --spacing-wizard-max-w, --spacing-card-gap) added to src/index.css"
    - "Phase 1 lifecycle palette tokens (--color-lifecycle-*) and Phase 1 spacing tokens (--spacing-gantt-*) preserved unchanged"
    - "All primitives use shadcn-style stone+green palette + green-700 focus ring + 1px borders"
  artifacts:
    - path: "src/ui/cn.ts"
      provides: "Tailwind class composition helper (clsx + tailwind-merge)"
      contains: "cn"
    - path: "src/ui/Button.tsx"
      provides: "Variants: primary, secondary (ghost-bordered), ghost, destructive; sizes: sm, md, lg; supports asChild"
      contains: "Button"
    - path: "src/ui/Dialog.tsx"
      provides: "Radix Dialog with shadcn-style DialogContent + DialogHeader + DialogTitle + DialogClose composition"
      contains: "DialogPrimitive, DialogContent"
    - path: "src/index.css"
      provides: "Phase 1 + Phase 2 tokens; lifecycle palette unchanged"
      contains: "--spacing-card-min, --spacing-modal-max-w, --spacing-panel-w"
  key_links:
    - from: "src/ui/* primitives"
      to: "@radix-ui/react-dialog, @radix-ui/react-dropdown-menu, etc"
      via: "import * as DialogPrimitive from '@radix-ui/react-dialog'"
      pattern: "from '@radix-ui/"
    - from: "All primitives"
      to: "src/ui/cn.ts"
      via: "import { cn } from './cn'"
      pattern: "import.*cn.*from.*'\\./cn'"
---

<objective>
Ship the shadcn-style copy-paste UI primitive set Wave 3 features depend on. Plus Phase 2's
Tailwind v4 token additions in @theme. No npx shadcn init (UI-SPEC §Design System rationale —
Tailwind v4 CSS-first incompatibility); hand-author each primitive against Radix.

Purpose: Setup Wizard, CatalogBrowser, PlantCard, CustomPlantModal, MyPlanPanel, MyPlanPill,
SettingsPanel, ImportPreviewModal — all rely on Button, Input, Label, Select, Dialog,
DropdownMenu, Switch, Toast, Card, Badge primitives with consistent styling. Hand-roll once,
consume everywhere.

Output:
  - src/ui/ directory with 11 files (cn.ts + 10 primitive components)
  - src/index.css extended with 6 new --spacing-* tokens (Phase 1 tokens preserved)
  - npm dependencies added: @radix-ui/react-{dialog,dropdown-menu,select,switch,toast,label,slot}, clsx, tailwind-merge
  - No tests at this layer (deferred to Plan 02-12 component tests; primitives are thin wrappers around Radix which is itself well-tested)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-layer-first-end-to-end/02-UI-SPEC.md
@.planning/phases/02-data-layer-first-end-to-end/02-PATTERNS.md
@.planning/phases/02-data-layer-first-end-to-end/02-RESEARCH.md

@src/index.css

<interfaces>
<!-- Tailwind v4 @theme additions (UI-SPEC §Spacing — Phase 2 additions) -->
```css
@theme {
  /* Phase 1 unchanged — DO NOT modify --color-lifecycle-* or --spacing-gantt-* */

  /* Phase 2 spacing additions */
  --spacing-card-min: 240px;
  --spacing-card-gap: 16px;
  --spacing-modal-max-w: 640px;
  --spacing-panel-w: 400px;
  --spacing-pill-h: 36px;
  --spacing-wizard-max-w: 720px;
}
```

<!-- Primitive APIs (mirror shadcn's published patterns) -->

```typescript
// cn.ts
export function cn(...inputs: ClassValue[]): string;

// Button.tsx
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'destructive';
  size?: 'sm' | 'md' | 'lg';
  asChild?: boolean;
}
export const Button: React.ForwardRefExoticComponent<ButtonProps>;

// Input.tsx — same signature as <input>; 40px height; stone-200 border, green-700 focus ring
export const Input: React.ForwardRefExoticComponent<React.InputHTMLAttributes<HTMLInputElement>>;

// Label.tsx — Radix Label primitive wrapper
export const Label: React.ForwardRefExoticComponent<...>;

// Dialog.tsx — Radix Dialog composition
export const Dialog;            // = DialogPrimitive.Root
export const DialogTrigger;
export const DialogPortal;
export const DialogOverlay;     // styled overlay
export const DialogContent;     // styled content with scrim + close X
export const DialogHeader;
export const DialogTitle;
export const DialogDescription;
export const DialogClose;
export const DialogFooter;

// Select.tsx, DropdownMenu.tsx, Switch.tsx, Toast.tsx, Card.tsx, Badge.tsx — analogous patterns
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix dependencies, create src/ui/cn.ts + Button + Input + Label + Card + Badge (presentational primitives, no Radix)</name>
  <files>package.json, src/ui/cn.ts, src/ui/Button.tsx, src/ui/Input.tsx, src/ui/Label.tsx, src/ui/Card.tsx, src/ui/Badge.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/02-data-layer-first-end-to-end/02-UI-SPEC.md §Design System + §Spacing + §Color (specifically Phase 2 additions and color tokens)
    - .planning/phases/02-data-layer-first-end-to-end/02-RESEARCH.md §Standard Stack lines 168-179 (exact Radix versions to install)
    - .planning/phases/02-data-layer-first-end-to-end/02-PATTERNS.md `src/ui/cn.ts (NEW)` and `src/ui/Button.tsx (NEW)` sections
    - src/index.css (Phase 1 tokens — must not modify)
  </read_first>
  <action>
    Step 1 — Install dependencies. Single command from RESEARCH §Standard Stack lines 209-217:
    ```
    npm install zod \
      @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-select \
      @radix-ui/react-checkbox @radix-ui/react-switch @radix-ui/react-label \
      @radix-ui/react-toast @radix-ui/react-slot \
      clsx tailwind-merge
    ```
    Note: zod is already installed by Plan 02-01; the redundancy is fine (npm dedupes). Verify after install: package.json contains all listed deps.

    Step 2 — Update src/index.css. Read first to capture Phase 1 tokens. Then APPEND Phase 2 spacing tokens inside the existing `@theme {}` block:

    ```css
    @theme {
      /* Phase 1 — LOCKED. Do not modify. */
      --color-lifecycle-indoor-start: #3B82F6;
      --color-lifecycle-harden-off: #EAB308;
      /* ...existing... */
      --spacing-gantt-row-height: 32px;
      --spacing-gantt-row-gap: 8px;
      --spacing-gantt-bar-height: 20px;
      --spacing-gantt-axis-height: 32px;
      --spacing-gantt-label-width: 140px;

      /* Phase 2 additions (UI-SPEC §Spacing) */
      --spacing-card-min: 240px;
      --spacing-card-gap: 16px;
      --spacing-modal-max-w: 640px;
      --spacing-panel-w: 400px;
      --spacing-pill-h: 36px;
      --spacing-wizard-max-w: 720px;
    }
    ```

    Step 3 — Create src/ui/cn.ts:
    ```typescript
    import { clsx, type ClassValue } from 'clsx';
    import { twMerge } from 'tailwind-merge';

    /** Compose Tailwind class names with conflict resolution. shadcn convention. */
    export function cn(...inputs: ClassValue[]): string {
      return twMerge(clsx(inputs));
    }
    ```

    Step 4 — Create src/ui/Button.tsx (UI-SPEC tokens: green-700 primary, red-700 destructive, stone-200 border ghost):
    ```typescript
    import { forwardRef, type ButtonHTMLAttributes } from 'react';
    import { Slot } from '@radix-ui/react-slot';
    import { cn } from './cn';

    export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'ghost' | 'destructive';
      size?: 'sm' | 'md' | 'lg';
      asChild?: boolean;
    }

    const VARIANT_CLASSES: Record<NonNullable<ButtonProps['variant']>, string> = {
      primary: 'bg-green-700 text-white hover:bg-green-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-700',
      secondary: 'bg-white text-stone-900 border border-stone-200 hover:border-stone-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-700',
      ghost: 'bg-transparent text-stone-600 hover:text-stone-900 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-700',
      destructive: 'bg-red-700 text-white hover:bg-red-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-700',
    };

    const SIZE_CLASSES: Record<NonNullable<ButtonProps['size']>, string> = {
      sm: 'h-8 px-3 text-sm font-medium rounded-md',
      md: 'h-10 px-4 text-sm font-medium rounded-md',
      lg: 'h-12 px-6 text-base font-semibold rounded-md',
    };

    export const Button = forwardRef<HTMLButtonElement, ButtonProps>(function Button(
      { variant = 'primary', size = 'md', asChild = false, className, ...props },
      ref,
    ) {
      const Comp = asChild ? Slot : 'button';
      return (
        <Comp
          ref={ref}
          className={cn(
            'inline-flex items-center justify-center gap-2 transition-colors disabled:opacity-50 disabled:cursor-not-allowed',
            VARIANT_CLASSES[variant],
            SIZE_CLASSES[size],
            className,
          )}
          {...props}
        />
      );
    });
    ```

    Step 5 — Create src/ui/Input.tsx (UI-SPEC: 40px height, stone-200 border, green-700 focus):
    ```typescript
    import { forwardRef, type InputHTMLAttributes } from 'react';
    import { cn } from './cn';

    export const Input = forwardRef<HTMLInputElement, InputHTMLAttributes<HTMLInputElement>>(
      function Input({ className, ...props }, ref) {
        return (
          <input
            ref={ref}
            className={cn(
              'h-10 w-full rounded-md border border-stone-200 bg-white px-3 text-base text-stone-900 placeholder:text-stone-500',
              'focus-visible:outline-2 focus-visible:outline-offset-0 focus-visible:outline-green-700 focus-visible:border-green-700',
              'disabled:opacity-50 disabled:cursor-not-allowed',
              'aria-[invalid=true]:border-red-700',
              className,
            )}
            {...props}
          />
        );
      },
    );
    ```

    Step 6 — Create src/ui/Label.tsx using Radix Label:
    ```typescript
    import { forwardRef, type ComponentPropsWithoutRef, type ElementRef } from 'react';
    import * as LabelPrimitive from '@radix-ui/react-label';
    import { cn } from './cn';

    export const Label = forwardRef<
      ElementRef<typeof LabelPrimitive.Root>,
      ComponentPropsWithoutRef<typeof LabelPrimitive.Root>
    >(function Label({ className, ...props }, ref) {
      return (
        <LabelPrimitive.Root
          ref={ref}
          className={cn('text-sm font-medium text-stone-900 leading-snug', className)}
          {...props}
        />
      );
    });
    ```

    Step 7 — Create src/ui/Card.tsx (presentational; UI-SPEC §4 plant card structure):
    ```typescript
    import { forwardRef, type HTMLAttributes } from 'react';
    import { cn } from './cn';

    export const Card = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
      function Card({ className, ...props }, ref) {
        return (
          <div
            ref={ref}
            className={cn(
              'rounded-md border border-stone-200 bg-white p-4 transition-colors',
              className,
            )}
            {...props}
          />
        );
      },
    );

    export const CardHeader = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
      function CardHeader({ className, ...props }, ref) {
        return <div ref={ref} className={cn('flex items-center justify-between gap-2', className)} {...props} />;
      },
    );

    export const CardBody = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
      function CardBody({ className, ...props }, ref) {
        return <div ref={ref} className={cn('mt-3', className)} {...props} />;
      },
    );

    export const CardFooter = forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>(
      function CardFooter({ className, ...props }, ref) {
        return <div ref={ref} className={cn('mt-4', className)} {...props} />;
      },
    );
    ```

    Step 8 — Create src/ui/Badge.tsx (UI-SPEC §Color new semantic accents — frost-tolerance, season, custom):
    ```typescript
    import { forwardRef, type HTMLAttributes } from 'react';
    import { cn } from './cn';

    export interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
      variant?:
        | 'tender' | 'half-hardy' | 'hardy'
        | 'cool' | 'warm'
        | 'custom' | 'permapeople'
        | 'manual';
    }

    const VARIANT_CLASSES: Record<NonNullable<BadgeProps['variant']>, string> = {
      tender: 'bg-red-100 text-red-800',
      'half-hardy': 'bg-amber-100 text-amber-800',
      hardy: 'bg-green-100 text-green-900',
      cool: 'bg-blue-100 text-blue-900',
      warm: 'bg-orange-100 text-orange-900',
      custom: 'bg-neutral-200 text-neutral-800',
      permapeople: 'bg-violet-100 text-violet-700',
      manual: 'bg-sky-100 text-sky-700',
    };

    export const Badge = forwardRef<HTMLSpanElement, BadgeProps>(
      function Badge({ variant = 'custom', className, ...props }, ref) {
        return (
          <span
            ref={ref}
            className={cn(
              'inline-flex items-center px-2 py-0.5 rounded-full text-sm font-semibold uppercase tracking-wider',
              VARIANT_CLASSES[variant],
              className,
            )}
            {...props}
          />
        );
      },
    );
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src/ui/cn.ts src/ui/Button.tsx src/ui/Input.tsx src/ui/Label.tsx src/ui/Card.tsx src/ui/Badge.tsx src/index.css 2>/dev/null; npx eslint src/ui/cn.ts src/ui/Button.tsx src/ui/Input.tsx src/ui/Label.tsx src/ui/Card.tsx src/ui/Badge.tsx &amp;&amp; grep -q '"@radix-ui/react-dialog":' package.json &amp;&amp; grep -q '"@radix-ui/react-label":' package.json &amp;&amp; grep -q '"@radix-ui/react-slot":' package.json &amp;&amp; grep -q '"clsx":' package.json &amp;&amp; grep -q '"tailwind-merge":' package.json &amp;&amp; grep -q -- '--spacing-card-min' src/index.css &amp;&amp; grep -q -- '--spacing-modal-max-w' src/index.css &amp;&amp; grep -q -- '--color-lifecycle-indoor-start' src/index.css &amp;&amp; grep -q 'export function cn' src/ui/cn.ts &amp;&amp; grep -q 'export const Button' src/ui/Button.tsx &amp;&amp; grep -q 'export const Badge' src/ui/Badge.tsx</automated>
  </verify>
  <done>cn helper + 5 presentational primitives compiled & lint-clean; deps installed; Tailwind tokens added without breaking Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Radix-backed primitives — Dialog + DropdownMenu</name>
  <files>src/ui/Dialog.tsx, src/ui/DropdownMenu.tsx</files>
  <read_first>
    - .planning/phases/02-data-layer-first-end-to-end/02-UI-SPEC.md §Component Inventory items 5, 6, 9, 11 (specifies which Radix primitives are wrapped where)
    - Radix UI docs (live): https://www.radix-ui.com/primitives/docs/components/{dialog,dropdown-menu,select,switch,toast}
    - shadcn/ui published copy-paste reference: github.com/shadcn-ui/ui/blob/main/apps/www/registry/default/ui/{dialog,dropdown-menu,select,switch,toast}.tsx (use as IMPLEMENTATION REFERENCE — adapt to our cn helper + Tailwind v4 tokens)
  </read_first>
  <action>
    Create both Dialog.tsx and DropdownMenu.tsx with shadcn-style composition: forward parts of the Radix primitive that consumers need, applying our Tailwind v4 + stone+green palette via cn() and the new spacing tokens. The shadcn registry components are MIT-licensed copy-paste templates; use them as the implementation source.

    **Dialog.tsx** — exemplar shape (full implementation):
    ```typescript
    import * as DialogPrimitive from '@radix-ui/react-dialog';
    import { forwardRef, type ComponentPropsWithoutRef, type ElementRef } from 'react';
    import { X } from 'lucide-react';
    import { cn } from './cn';

    export const Dialog = DialogPrimitive.Root;
    export const DialogTrigger = DialogPrimitive.Trigger;
    export const DialogPortal = DialogPrimitive.Portal;
    export const DialogClose = DialogPrimitive.Close;

    export const DialogOverlay = forwardRef<
      ElementRef<typeof DialogPrimitive.Overlay>,
      ComponentPropsWithoutRef<typeof DialogPrimitive.Overlay>
    >(function DialogOverlay({ className, ...props }, ref) {
      return (
        <DialogPrimitive.Overlay
          ref={ref}
          className={cn(
            'fixed inset-0 z-50 bg-stone-900/40',
            'data-[state=open]:animate-in data-[state=closed]:animate-out',
            'data-[state=open]:fade-in-0 data-[state=closed]:fade-out-0',
            className,
          )}
          {...props}
        />
      );
    });

    export const DialogContent = forwardRef<
      ElementRef<typeof DialogPrimitive.Content>,
      ComponentPropsWithoutRef<typeof DialogPrimitive.Content>
    >(function DialogContent({ className, children, ...props }, ref) {
      return (
        <DialogPortal>
          <DialogOverlay />
          <DialogPrimitive.Content
            ref={ref}
            className={cn(
              'fixed left-1/2 top-1/2 z-50 -translate-x-1/2 -translate-y-1/2',
              'w-[calc(100vw-32px)] max-w-[var(--spacing-modal-max-w)]',
              'rounded-md bg-white p-6 shadow-lg border border-stone-200',
              'data-[state=open]:animate-in data-[state=closed]:animate-out',
              className,
            )}
            {...props}
          >
            {children}
            <DialogPrimitive.Close className="absolute right-4 top-4 rounded-sm opacity-70 hover:opacity-100 focus:outline-2 focus:outline-green-700">
              <X className="h-5 w-5" />
              <span className="sr-only">Close</span>
            </DialogPrimitive.Close>
          </DialogPrimitive.Content>
        </DialogPortal>
      );
    });

    export const DialogHeader = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
      <div className={cn('flex flex-col gap-1.5 text-left', className)} {...props} />
    );

    export const DialogFooter = ({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) => (
      <div className={cn('flex justify-end gap-2 mt-6', className)} {...props} />
    );

    export const DialogTitle = forwardRef<
      ElementRef<typeof DialogPrimitive.Title>,
      ComponentPropsWithoutRef<typeof DialogPrimitive.Title>
    >(function DialogTitle({ className, ...props }, ref) {
      return (
        <DialogPrimitive.Title
          ref={ref}
          className={cn('text-xl font-semibold text-stone-900', className)}
          {...props}
        />
      );
    });

    export const DialogDescription = forwardRef<
      ElementRef<typeof DialogPrimitive.Description>,
      ComponentPropsWithoutRef<typeof DialogPrimitive.Description>
    >(function DialogDescription({ className, ...props }, ref) {
      return (
        <DialogPrimitive.Description
          ref={ref}
          className={cn('text-sm text-stone-600', className)}
          {...props}
        />
      );
    });
    ```

    **DropdownMenu.tsx** — apply the same shape:

    - Exports: Root (alias DropdownMenu), Trigger, Portal, Content, Item, Separator
    - Style content: `bg-white border border-stone-200 rounded-md shadow-md py-1`
    - Items: `px-3 py-1.5 text-sm hover:bg-stone-50 cursor-pointer`
    - Destructive item variant: `text-red-700 hover:bg-red-50` (consumer applies via className)
    - Reference: github.com/shadcn-ui/ui/blob/main/apps/www/registry/default/ui/dropdown-menu.tsx

    Each file: ~80-120 LOC. Use forwardRef where Radix expects ref forwarding. Use `cn()` for all class composition.

    NOTE: This task does NOT install lucide-react (already in Phase 1 deps per package.json). It does USE: X icon (Dialog close).

    NOTE: data-[state=open]:animate-in classes assume `tailwindcss-animate` plugin OR raw CSS animations. Tailwind v4 may not include these out of the box. If `tailwindcss-animate` is needed, install it: `npm install tailwindcss-animate` in this task (Task 2). Verify if v4 obviates this; if v4 has built-in animations via @theme keyframes, prefer that path. Document the chosen path in the file header.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src/ui/Dialog.tsx src/ui/DropdownMenu.tsx &amp;&amp; grep -q '@radix-ui/react-dialog' src/ui/Dialog.tsx &amp;&amp; grep -q '@radix-ui/react-dropdown-menu' src/ui/DropdownMenu.tsx &amp;&amp; grep -q 'export const DialogContent' src/ui/Dialog.tsx &amp;&amp; grep -q 'export const DropdownMenuItem\|export const DropdownMenuContent' src/ui/DropdownMenu.tsx</automated>
  </verify>
  <done>Dialog + DropdownMenu primitives compiled clean; shadcn export pattern; cn() used; spacing tokens consumed via `var(--spacing-modal-max-w)` arbitrary-value syntax.</done>
</task>


<task type="auto">
  <name>Task 3: Radix-backed primitives — Select + Switch + Toast</name>
  <files>src/ui/Select.tsx, src/ui/Switch.tsx, src/ui/Toast.tsx</files>
  <read_first>
    - .planning/phases/02-data-layer-first-end-to-end/02-UI-SPEC.md §Component Inventory items 5, 6, 9, 11 (specifies which Radix primitives are wrapped where)
    - Radix UI docs (live): https://www.radix-ui.com/primitives/docs/components/{select,switch,toast}
    - shadcn/ui published copy-paste reference: github.com/shadcn-ui/ui/blob/main/apps/www/registry/default/ui/{select,switch,toast}.tsx (use as IMPLEMENTATION REFERENCE — adapt to our cn helper + Tailwind v4 tokens)
    - src/ui/Dialog.tsx (Task 2 — same shape pattern; use as a local reference)
  </read_first>
  <action>
    For each primitive, create the file with shadcn-style composition: forward parts of the Radix primitive that consumers need, applying our Tailwind v4 + stone+green palette via cn() and the new spacing tokens. Same shape as Dialog/DropdownMenu in Task 2.

    **Select.tsx**: Root, Trigger, Content, Item, ItemText, ItemIndicator, Value, Group, Label.
    - Trigger styled as Input (40px height, stone-200 border, green-700 focus): `inline-flex items-center justify-between w-full h-10 rounded-md border border-stone-200 bg-white px-3 text-base text-stone-900 focus-visible:outline-2 focus-visible:outline-green-700 focus-visible:border-green-700`
    - Content: `bg-white border border-stone-200 rounded-md shadow-md min-w-[var(--radix-select-trigger-width)]`
    - Items: `px-3 py-2 hover:bg-stone-50 cursor-pointer flex items-center gap-2`
    - Use `lucide-react`'s `ChevronDown` for trigger right-icon and `Check` for selected indicator.

    **Switch.tsx**: Root only.
    - Track: `inline-flex h-6 w-11 items-center rounded-full bg-stone-300 data-[state=checked]:bg-green-700 transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-700`
    - Thumb: `block h-5 w-5 rounded-full bg-white shadow-sm transition-transform translate-x-0.5 data-[state=checked]:translate-x-[1.375rem]`

    **Toast.tsx**: Root, Action, Close, Title, Description, Provider, Viewport.
    - Variants per UI-SPEC §11: success (`bg-green-700 text-white`), warning (`bg-amber-100 text-amber-900 border border-amber-200`), error (`bg-red-700 text-white`).
    - Viewport: `fixed bottom-4 right-4 z-50 flex flex-col gap-2 max-w-[420px] outline-none`
    - Cap stack at 3 by limiting `Provider duration` + `swipeDirection="right"` + parent component logic (UI-SPEC §11).
    - Auto-dismiss 4s default (set on Provider via `duration={4000}`); multi-tab variant uses `duration={Infinity}` consumer-side.

    Each file: ~80-120 LOC. Use forwardRef where Radix expects ref forwarding. Use `cn()` for all class composition. Same animation handling as Task 2 (tailwindcss-animate already installed in Task 2 if needed).

    Icons used: ChevronDown, Check (Select).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src/ui/Select.tsx src/ui/Switch.tsx src/ui/Toast.tsx &amp;&amp; grep -q '@radix-ui/react-select' src/ui/Select.tsx &amp;&amp; grep -q '@radix-ui/react-switch' src/ui/Switch.tsx &amp;&amp; grep -q '@radix-ui/react-toast' src/ui/Toast.tsx &amp;&amp; grep -q 'export const SelectTrigger\|export const SelectContent' src/ui/Select.tsx &amp;&amp; grep -q 'export const Switch' src/ui/Switch.tsx &amp;&amp; grep -q 'export const Toast\|export const ToastViewport' src/ui/Toast.tsx</automated>
  </verify>
  <done>Select + Switch + Toast primitives compiled clean; shadcn export pattern; cn() used; UI-SPEC §11 toast variants implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied className strings → cn() | Mitigated by tailwind-merge (sanitizes conflicts; doesn't sanitize XSS — but className is never injected as innerHTML). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Tampering | className override | accept | Consumers control className; primitives forward via cn(). No untrusted text reaches innerHTML. |
| T-02-24 | DoS | Toast spam | mitigate | Toast viewport caps stack to 3 (UI-SPEC §11). |
</threat_model>

<verification>
- 11 files exist in src/ui/
- npx tsc --noEmit clean
- npx eslint src/ui/ clean
- package.json lists all 8 Radix packages + clsx + tailwind-merge
- src/index.css has 6 Phase 2 spacing tokens AND retains Phase 1 lifecycle palette + gantt spacing
- Phase 1 tests still pass (`npm test -- --run` to be safe)
</verification>

<success_criteria>
- All 11 src/ui/* files exist and compile
- Tailwind v4 tokens extended without breaking Phase 1
- shadcn-style stone+green palette consistently applied across primitives
- Radix primitives wrapped (no hand-rolled focus traps)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-first-end-to-end/02-07-SUMMARY.md`
</output>
