---
phase: 04-polish-mobile-ship
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - public/_headers
  - tests/fixtures/200-event-stress.ts
  - tests/integration/stress-fixture.test.ts
  - .planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-03
  - POL-07
user_setup:
  - service: cloudflare-pages
    why: "Static-site deploy with CI/CD for DEPLOY-01"
    account_setup:
      - "User must have a Cloudflare account (free tier sufficient)"
      - "User must connect the GitHub repo to a new Cloudflare Pages project via the dashboard (one-time OAuth)"
    dashboard_config:
      - task: "Create new Pages project linked to the GitHub repo"
        location: "Cloudflare dashboard → Workers & Pages → Create → Pages → Connect to Git"
      - task: "Set production branch = main"
        location: "Project settings → Builds & deployments"
      - task: "Set build command = npm run build"
        location: "Project settings → Builds & deployments"
      - task: "Set build output directory = dist"
        location: "Project settings → Builds & deployments"
      - task: "Set NODE_VERSION env var = 20 (or higher; Vite 7+/TS 6 require Node 20+)"
        location: "Project settings → Environment variables → Production"
must_haves:
  truths:
    - "public/_headers committed with /index.html no-cache rule AND /assets/* immutable rule (DEPLOY-03 + RESEARCH §Pattern 5)"
    - "After `npm run build`, dist/_headers exists at the deployed root (Vite copies public/* to dist/ — RESEARCH §Pitfall 6)"
    - "tests/fixtures/200-event-stress.ts exports a GardenPlan with ~40 plantings producing ~200 schedule events (POL-07 fixture per CONTEXT specifics)"
    - "Stress fixture includes mix of indoor-start, transplant, direct-sow, harvest-window event types (RESEARCH Pitfall 8 — heterogeneous coverage)"
    - "Stress fixture loadable into the app via tests/integration/stress-fixture.test.ts and renders without throwing"
    - "Manual perf checkpoint: DevTools Performance trace at /plan with stress fixture loaded shows median frame >= 50fps during simulated arrow-key drag (POL-07 60fps target with reasonable margin)"
    - "Manual deploy verification: push to main → Cloudflare Pages auto-deploys → curl -I against deployed /index.html returns 'cache-control: no-cache' (DEPLOY-03 verification per RESEARCH Pitfall 6)"
    - "Deployed app loads at https://{project}.pages.dev with all Phase 4 features working: setup wizard, gantt, calendar, tasks, settings, mobile responsiveness"
  artifacts:
    - path: "public/_headers"
      provides: "Cloudflare Pages cache headers"
      contains: "no-cache"
    - path: "tests/fixtures/200-event-stress.ts"
      provides: "POL-07 perf stress fixture (~40 plantings, ~200 events)"
      exports: ["stressFixture"]
    - path: ".planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md"
      provides: "Deploy verification log + perf checkpoint results + screenshots/curl outputs"
  key_links:
    - from: "public/_headers"
      to: "dist/_headers (post-build)"
      via: "vite copies public/* to dist/ automatically"
      pattern: "no-cache"
    - from: "tests/fixtures/200-event-stress.ts"
      to: "src/samplePlan.ts"
      via: "multiplied via succession + plant variety per CONTEXT specifics"
      pattern: "samplePlan"
---

<objective>
Final wave: ship POL-07 perf checkpoint, DEPLOY-01 (Cloudflare Pages CI/CD), DEPLOY-03 (cache headers). Three checkpoints + one autonomous task. This is the gate before phase verification.

Purpose: Phase 4's last delivery: the live URL. Everything earlier has been preparation.

Output: Stress fixture committed, _headers committed, manual perf trace captured, Cloudflare Pages project connected by user, deployed URL verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-polish-mobile-ship/04-CONTEXT.md
@.planning/phases/04-polish-mobile-ship/04-RESEARCH.md
@.planning/phases/04-polish-mobile-ship/04-PATTERNS.md
@.planning/phases/04-polish-mobile-ship/04-UI-SPEC.md

@src/samplePlan.ts
@src/domain/types.ts
@public/_redirects
@package.json
@vite.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stress fixture (~200 events) + public/_headers + smoke test</name>
  <files>public/_headers, tests/fixtures/200-event-stress.ts, tests/integration/stress-fixture.test.ts</files>

  <read_first>
    - src/samplePlan.ts (existing seed plan; multiplied via succession + plants for stress fixture per CONTEXT specifics)
    - src/domain/types.ts (GardenPlan, Planting, EventType — confirm types for fixture)
    - public/_redirects (CF Pages convention file analog per PATTERNS — same shape conventions)
    - .planning/phases/04-polish-mobile-ship/04-PATTERNS.md §tests/fixtures/200-event-stress.ts + §public/_headers
    - .planning/phases/04-polish-mobile-ship/04-RESEARCH.md §Pattern 5 (cache headers verbatim) + §Pitfall 6 (file location must be public/) + §Pitfall 8 (heterogeneous fixture)
    - .planning/phases/04-polish-mobile-ship/04-CONTEXT.md specifics (40 plantings × ~5 events; mix of indoor-start, transplant, direct-sow, harvest-window event types)
    - vite.config.ts (confirm assetsDir is default 'assets'; if not, _headers /assets/* rule needs adjustment per RESEARCH Assumption A1)
  </read_first>

  <behavior>
    - public/_headers contains the literal:
      ```
      /index.html
        Cache-Control: no-cache, no-store, must-revalidate
      /assets/*
        Cache-Control: public, max-age=31536000, immutable
      ```
    - After `npm run build`, dist/_headers file exists with the same content (Vite copies public/*).
    - tests/fixtures/200-event-stress.ts exports `stressFixture: GardenPlan` with ~40 plantings.
    - The fixture, when passed through generateSchedule(), produces 150-250 schedule events (loose range; ~200 is target, exact varies with plant traits).
    - The fixture exercises at least 4 distinct event types: indoor-start (or seed-start), transplant, direct-sow (if applicable), harvest-window.
    - Smoke test loads the fixture into a fresh planStore via replacePlan(stressFixture), runs generateSchedule(), and asserts the resulting events array length >= 150 AND <= 250 AND includes >= 4 distinct event types.
  </behavior>

  <action>
    Step 1 — `public/_headers` per RESEARCH §Pattern 5 verbatim:
    ```
    # Cloudflare Pages cache headers — DEPLOY-03
    # Source: https://developers.cloudflare.com/pages/configuration/headers/

    # index.html must NOT cache so a deploy is reflected on the next page load.
    /index.html
      Cache-Control: no-cache, no-store, must-revalidate

    # Vite emits hashed filenames in /assets/ by default (e.g. index-A1b2C3.js).
    # Content-addressed → safe to cache aggressively + immutably.
    /assets/*
      Cache-Control: public, max-age=31536000, immutable
    ```

    If `vite.config.ts` overrides assetsDir to something other than 'assets', adjust the second rule to match the actual emitted directory.

    Step 2 — `tests/fixtures/200-event-stress.ts` per PATTERNS analog (samplePlan.ts) + CONTEXT specifics:

    Read src/samplePlan.ts to understand the existing GardenPlan shape. Build a stress fixture by multiplying:
    - Use the existing 4 catalog plants (or all 50 from Phase 2) as base plant IDs.
    - Generate 40 plantings split across plant types: ~10 each of tomato/lettuce/broccoli/garlic (or use Phase 2 plants for more variety per Pitfall 8).
    - Enable succession on every 3rd planting (drives event count up via Phase 2 succession engine).
    - Mix anchor dates to spread across the season for non-trivial render geometry.

    ```typescript
    import { samplePlan } from '../../src/samplePlan';
    import type { GardenPlan, Planting } from '../../src/domain/types';

    const PLANT_IDS = ['tomato', 'lettuce', 'broccoli', 'garlic'] as const;
    // Phase 2 expanded the catalog to 50; use up to 8 plants for variety per RESEARCH Pitfall 8
    // (heterogeneous coverage — mix of indoor-start, transplant, direct-sow, harvest-window)

    function buildPlantings(count: number): Planting[] {
      return Array.from({ length: count }, (_, i) => ({
        id: `stress-${i}`,
        plantId: PLANT_IDS[i % PLANT_IDS.length],
        successionEnabled: i % 3 === 0,
        successionIndex: 0,
        locks: {},
      }));
    }

    export const stressFixture: GardenPlan = {
      ...samplePlan,
      plantings: buildPlantings(40),
    };
    ```

    Adjust the spread/shape to match the actual Planting type (read types.ts for required fields). If `successionEnabled` or `locks` are not on the Planting type, omit them.

    Step 3 — `tests/integration/stress-fixture.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { stressFixture } from '../fixtures/200-event-stress';
    import { generateSchedule } from '../../src/domain/scheduler';
    import { catalog } from '../../src/data/catalog'; // or wherever catalog lives

    describe('200-event stress fixture', () => {
      it('produces 150-250 schedule events from ~40 plantings', () => {
        const events = generateSchedule(stressFixture, catalog);
        expect(events.length).toBeGreaterThanOrEqual(150);
        expect(events.length).toBeLessThanOrEqual(300);
      });
      it('exercises at least 4 distinct event types', () => {
        const events = generateSchedule(stressFixture, catalog);
        const distinctTypes = new Set(events.map(e => e.type));
        expect(distinctTypes.size).toBeGreaterThanOrEqual(4);
      });
      it('runs without throwing', () => {
        expect(() => generateSchedule(stressFixture, catalog)).not.toThrow();
      });
    });
    ```

    Adjust import paths to match actual generateSchedule + catalog export locations. If the threshold ranges don't match reality, adjust and document — primary goal is "produces a non-trivial event count".
  </action>

  <verify>
    <automated>npm run build && test -f dist/_headers && grep -q "no-cache" dist/_headers && npm test -- --run tests/integration/stress-fixture.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `test -f public/_headers` returns 0 exit code (file exists)
    - `grep -c "no-cache" public/_headers` >= 1
    - `grep -c "max-age=31536000" public/_headers` >= 1
    - `grep -c "immutable" public/_headers` >= 1
    - `test -f tests/fixtures/200-event-stress.ts` returns 0
    - `grep -c "stressFixture" tests/fixtures/200-event-stress.ts` >= 1
    - `npm run build` exits 0
    - `test -f dist/_headers` returns 0 (Vite copied public/* into dist/)
    - `grep -c "no-cache" dist/_headers` >= 1
    - `npm test -- --run tests/integration/stress-fixture.test.ts` exits 0
  </acceptance_criteria>

  <done>_headers in public/ + dist/; stress fixture loadable + produces ~200 events spanning 4+ types</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: POL-07 manual perf checkpoint — 60fps drag at 200 events</name>

  <what-built>
    Stress fixture (Task 1) + Phase 3 rAF throttling + memoized per-bar selectors (already shipped) + Phase 4 keyboard-drag (Plan 06).
  </what-built>

  <how-to-verify>
    Step 0 — Wire the `?stress=1` URL hook (one-time setup, dev-only — gated behind `import.meta.env.DEV` so it tree-shakes out of production builds):

    Edit `src/app/AppShell.tsx` (or the planStore-hydration entrypoint) to add at module top, before the AppShell render:

    ```typescript
    // Dev-only stress fixture hook — POL-07 perf checkpoint loader.
    // Tree-shaken from production builds via import.meta.env.DEV gate.
    if (import.meta.env.DEV && typeof window !== 'undefined' && new URLSearchParams(window.location.search).has('stress')) {
      void import('../../tests/fixtures/200-event-stress').then(({ stressFixture }) => {
        usePlanStore.getState().replacePlan(stressFixture);
      });
    }
    ```

    If `replacePlan` does not exist on planStore, use the existing setter that swaps the entire plan (likely `setPlan` or similar — read planStore.ts; if absent, add a thin `replacePlan(plan: GardenPlan)` setter that calls `set({ plan })` and bumps temporal/persist appropriately).

    Step 1 — Load the stress fixture into the running app:
    ```bash
    npm run dev
    ```
    Open http://localhost:5173/?stress=1#/plan in Chrome. The `?stress=1` query param triggers the dev-only hook from Step 0; planStore is replaced with the 200-event fixture before the gantt mounts.

    (Use `npm run dev` rather than `npm run preview` because the hook is gated on `import.meta.env.DEV` and only executes in the dev build. Vite's HMR + dev build still produces realistic per-frame perf within ~10% of preview for interactive measurement.)

    Step 2 — Open DevTools → Performance tab. Click Record. Perform:
    1. Tab into the gantt (skip-to-main → Tab to first bar)
    2. Hold ArrowRight for ~3 seconds (rapid pending-edit staging via useKeyboardBarDrag)
    3. Press Enter to commit
    4. Press ArrowLeft a few times then Esc to cancel
    5. Stop the recording

    Step 3 — Inspect the Performance trace:
    - Look at Frames row. Count green frames (60fps target = 16.7ms budget per frame).
    - Median frame time during arrow-key spam should be <= 20ms (= 50fps), ideally <= 16.7ms.
    - Allow occasional yellow frames (40-50ms) but no red frames (>= 50ms / "long task").
    - Check Main thread for any synchronous task >100ms — that's a fail.

    Step 4 — Optional: Run Lighthouse against /plan with stress fixture loaded; Performance score should be >= 75 on Slow 4G + Mid-tier mobile emulation.

    Step 5 — Record findings in `.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md` with: median frame time, max frame time, screenshot of Performance trace, any noted optimization opportunities.
  </how-to-verify>

  <resume-signal>Type "perf-pass" with median + max frame numbers if 60fps target met (or close enough — median <= 20ms acceptable for v1 ship). If perf is unacceptable, type "perf-fail: [details]" with profiler findings; this triggers a remediation pass (likely a Plan 08 to add memoization or virtualization).</resume-signal>

  <files>.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</files>
  <action>Manual perf gate. Executor follows <how-to-verify> steps to load the stress fixture, capture a Performance trace during keyboard-drag spam, and record results. If median frame &gt; 20ms, surface remediation suggestions (memoization, virtualization, rAF batch tuning) and recommend a follow-up plan before resuming.</action>
  <verify>
    <automated>echo "Manual perf checkpoint — see 04-07-DEPLOY-NOTES.md for trace data" &amp;&amp; test -f .planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</automated>
  </verify>
  <done>Performance trace captured; median frame time documented in 04-07-DEPLOY-NOTES.md; 60fps target met (median ≤20ms) OR remediation plan proposed</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: DEPLOY-01 — User connects Cloudflare Pages project</name>

  <what-built>
    All Phase 4 code shipped to main. _headers in public/. Build passes locally.
  </what-built>

  <how-to-verify>
    This is the only TRULY-human-required step in Phase 4. Cloudflare Pages git integration requires the user to OAuth GitHub from the Cloudflare dashboard.

    Step 1 — Open https://dash.cloudflare.com/

    Step 2 — Workers & Pages → Create → Pages → Connect to Git

    Step 3 — Authorize Cloudflare to access your GitHub account; select the garden_gantt repo

    Step 4 — Configure the build:
    - Production branch: `main`
    - Build command: `npm run build`
    - Build output directory: `dist`
    - Root directory: leave blank (default = repo root)

    Step 5 — Add environment variable (required for Vite 7+ / TS 6):
    - NODE_VERSION = 20 (or 22 for safety)
    - Set in: Project settings → Environment variables → Production

    Step 6 — Save and Deploy. First build kicks off; takes ~2-4 minutes typically.

    Step 7 — Once green, the deployed URL is `https://{project-name}.pages.dev` (Cloudflare assigns the project name from the repo name).

    Step 8 — Record the deployed URL in `.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md`.
  </how-to-verify>

  <resume-signal>Paste the deployed URL (e.g., "deployed: https://garden-gantt.pages.dev") OR "deploy-fail: [error message]" if the CF Pages build fails (typical causes: Node version mismatch, missing env var, build script error).</resume-signal>

  <files>.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</files>
  <action>One-time human OAuth + dashboard configuration. Cloudflare Pages git integration cannot be triggered via CLI/API in any practical way (requires the user's GitHub OAuth + dashboard project creation). Executor instructs the user, waits for the deployed URL, and records it in DEPLOY-NOTES.md.</action>
  <verify>
    <automated>test -f .planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md &amp;&amp; grep -E "https://.*pages\.dev" .planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</automated>
  </verify>
  <done>Cloudflare Pages project connected to GitHub repo, first build green, deployed URL recorded in DEPLOY-NOTES.md</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: DEPLOY-03 verification + smoke deployed app</name>

  <what-built>
    Live deployed Cloudflare Pages URL with all Phase 4 features.
  </what-built>

  <how-to-verify>
    Step 1 — Verify cache headers on deployed URL (RESEARCH §Pitfall 6):
    ```bash
    curl -I https://{project}.pages.dev/index.html | grep -i cache-control
    ```
    Expected: `cache-control: no-cache, no-store, must-revalidate`

    ```bash
    curl -I https://{project}.pages.dev/assets/$(curl -s https://{project}.pages.dev/ | grep -oP 'assets/\S+?\.js' | head -1) | grep -i cache-control
    ```
    Expected: `cache-control: public, max-age=31536000, immutable`

    If either header is wrong, the `_headers` file didn't propagate. Re-check `dist/_headers` exists post-build and that Cloudflare Pages picked it up (Pages Functions → Headers tab in dashboard).

    Step 2 — Functional smoke at the deployed URL:
    1. /setup wizard works (enter ZIP → derives zone + frost dates)
    2. /plan renders gantt with the user's plant choices
    3. Drag a bar (desktop) — cascades and snaps to constraints
    4. Switch to /tasks → Today/This Week/Overdue sections present
    5. Switch to calendar tab → events visible
    6. Settings → Export plan → JSON downloads → confirmation toast (post-export side-effect)
    7. Settings → Reset onboarding → revisit /plan → coach mark 1 appears
    8. Settings → Clear plan → modal-confirm appears; cancel works
    9. Resize browser to <640px (or DevTools mobile emulation): default view becomes calendar; gantt has sticky plant column + tap-to-edit modal

    Step 3 — A11y smoke at the deployed URL:
    1. Tab from page top → Skip to main content link visible + focused
    2. Tab again → focuses something inside main
    3. Visit /plan, Tab to first bar, ArrowRight 3 times, Enter → commits via keyboard drag
    4. Esc on a constraint violation tooltip → tooltip dismisses

    Step 4 — Deploy propagation test:
    1. Make a trivial code change (e.g., add a comment to a file)
    2. Push to main
    3. Wait ~3 minutes for Cloudflare Pages to rebuild
    4. Hard-refresh the deployed URL → verify the change appears (proves DEPLOY-03 cache headers work)

    Step 5 — Record results in `04-07-DEPLOY-NOTES.md`: deployed URL, header curl outputs, smoke results, propagation test timing.
  </how-to-verify>

  <resume-signal>Type "deploy-verified" if all smoke + a11y + propagation checks pass; type "issue: [details]" with specifics if anything fails (e.g., "issue: header missing on /index.html — investigate _headers location" or "issue: gantt drag broken on iOS Safari").</resume-signal>

  <files>.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</files>
  <action>Manual deploy verification gate. Executor runs the curl + smoke + a11y + propagation checks per <how-to-verify> against the deployed URL from Task 3, records each result in DEPLOY-NOTES.md.</action>
  <verify>
    <automated>echo "Manual deploy-verify checkpoint — see 04-07-DEPLOY-NOTES.md for results" &amp;&amp; grep -c "no-cache" .planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md</automated>
  </verify>
  <done>Deployed URL serves /index.html with no-cache header; functional + a11y + propagation smoke tests recorded in DEPLOY-NOTES.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user push to main → Cloudflare Pages builder | git integration runs `npm run build` in CF's sandboxed builder; output uploaded to edge |
| Cloudflare edge → user browser | _headers controls cache behavior; HTTPS provided by CF |
| public/_headers → dist/_headers | Vite-managed copy; verified post-build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Spoofing | Stale-cache attack: user keeps seeing old bundle after deploy | mitigate | DEPLOY-03 _headers explicit no-cache on /index.html; Vite hashes assets so bundle name changes invalidate cache. Task 4 step 4 verifies via push + propagation test. |
| T-04-07-02 | Tampering | _headers file not copied to dist/ (RESEARCH Pitfall 6) | mitigate | Task 1 acceptance check: `test -f dist/_headers` after build. Task 4 step 1 verifies via curl on deployed URL. |
| T-04-07-03 | Repudiation | Cloudflare Pages build silently fails (e.g. Node version mismatch) | mitigate | Task 3 sets NODE_VERSION=20 explicitly. CF dashboard surfaces build failures in the deploys tab. |
| T-04-07-04 | Information Disclosure | preview deploys at commit-hash.pages.dev expose work-in-progress | accept | Per RESEARCH Open Question 4: same headers apply to all deploys; user can disable preview deploys per branch in CF dashboard if concerned. Single-user app; preview URLs not shared by default. |
| T-04-07-05 | Denial of Service | 200-event stress fixture exposes O(N²) algorithmic regression | mitigate | Task 2 manual perf check is the gate. If frame budget exceeded → remediation pass (likely add virtualization to GanttView) before phase verification. |
| T-04-07-06 | Elevation of Privilege | Cloudflare Pages git integration grants too much permission | mitigate | CF Pages OAuth requests minimum read scope on the connected repo by default. User can review during Task 3 Step 3 OAuth screen. |
</threat_model>

<verification>
- Task 1 automated tests pass: stress fixture loads + produces ~200 events; _headers in public/ AND dist/ post-build
- Task 2 perf checkpoint: median frame time <= 20ms during stress drag; max frame time documented
- Task 3 deploy: Cloudflare Pages project connected; first build green; live URL captured
- Task 4 verification: curl headers correct on deployed URL; functional smoke pass; push-to-deploy propagation works
- All recorded in `.planning/phases/04-polish-mobile-ship/04-07-DEPLOY-NOTES.md`
</verification>

<success_criteria>
- DEPLOY-01: live Cloudflare Pages URL deploys on push to main
- DEPLOY-03: index.html no-cache + assets immutable, verified via curl
- POL-07: 60fps target met (or near-met) on 200-event stress fixture
- Phase 4 phase-gate: ready for `/gsd-verify-phase 4`
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/04-polish-mobile-ship/04-07-SUMMARY.md` with deploy URL, perf numbers, and any issues surfaced
2. `04-07-DEPLOY-NOTES.md` should already contain detailed results from Tasks 2/3/4
</output>
