# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite
coverage
*.local
.env
*.tsbuildinfo

# Cloudflare Worker (cors-proxy/) artifacts
cors-proxy/.wrangler/
cors-proxy/.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# catalog.generated.ts IS committed — it's the live catalog source.
# Re-run `npm run import:catalog` after updating PDFs or parsers, then commit
# the regenerated file so production builds don't depend on running the ETL.
